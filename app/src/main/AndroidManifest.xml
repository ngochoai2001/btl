<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectcuoikhoa">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_app"
        android:label="Camera"
        android:roundIcon="@drawable/icon_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar">
        <activity android:name="activiti.Profile.LienHeVoiChungToi"></activity>
        <activity android:name="activiti.Profile.Setting" />
        <activity android:name="activiti.Profile.UpdateAvatar" />
        <activity android:name="activiti.LogIn" />
        <activity android:name="fragment.FragmentSearch" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".DiaChiCuaHang"
            android:label="@string/title_activity_dia_chi_cua_hang" />
        <activity
            android:name="activiti.LoaiSanPham.SanPhamHot"
            android:label="Sản phẩm HOT"
            android:parentActivityName="activiti.MainActivity" />
        <activity android:name="activiti.VanDeVeLogIn.HoaDon" />
        <activity android:name="activiti.Profile.DonHangDaMua" />
        <activity android:name="activiti.Profile.SuaThongTin" />
        <activity android:name="activiti.Profile.DanhSachYeuThich" />
        <activity
            android:name="activiti.VanDeVeLogIn.ResetPassword"
            android:parentActivityName="activiti.LogIn" />
        <activity android:name="activiti.Profile.ChangePassword" />
        <activity
            android:name="activiti.ChiTietSanPham"
            android:parentActivityName="activiti.MainActivity" />
        <activity
            android:name="activiti.OngKinh"
            android:label="Ống kính"
            android:parentActivityName="activiti.MainActivity" />
        <activity android:name="fragment.Fragment_Home" />
        <activity
            android:name="activiti.LoaiSanPham.SanPhamMoiNhat"
            android:label="Sản phẩm mới nhất"
            android:parentActivityName="activiti.MainActivity" />
        <activity
            android:name="activiti.MayAnh"
            android:label="Máy ảnh"
            android:parentActivityName="activiti.MainActivity" />
        <activity
            android:name="activiti.Cart"
            android:label="Giỏ hàng"
            android:parentActivityName="activiti.MainActivity" />
        <activity android:name="activiti.DangKi" />
        <activity android:name="activiti.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="activiti.MainActivity" />
    </application>

</manifest>